**Solve this challenge.** Your answer must be an explicit construction — the tuple (S, X, c, A, W). Output the sequence and its explicit folded coordinates X *first* (so the self-avoiding walk and every contact can be checked by hand), then the generator W written out, then the computations the deliverable asks for. Do not critique, summarize, or rewrite the challenge; produce the construction. If you can't satisfy every condition, name the one that blocks you — don't revise the problem.

The crux is C5, and it is won at **one designated copied site**, never by a longer strand (fidelity is per-site, Q = minᵢ qᵢ; lengthening the tape is disqualified). Give the site i*, a tape family T^(1), T^(2), T^(3) folding progressively deeper pockets around it, and its per-site fidelity q*(n) showing the error fall geometrically (1 − q*(n) ≲ C·d^(−n)).

Build first — a wall must be earned. If you conclude it is impossible, that is only an acceptable answer when you exhibit your best explicit construction (real coordinates, a real W), its computed q*(n), and the exact step where it stops improving and why it cannot escape that step. A bare "the coordination bound forbids it," with no construction that actually runs into the bound, is not an answer.