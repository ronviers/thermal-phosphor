# thermal-phosphor — handoff to next session

*Written 2026-07-22, updated 2026-07-23. Read [`../CLAUDE.md`](../CLAUDE.md) (the guards) and [`../STATE.md`](../STATE.md) (the thinking) first — this is "what changed and where to pick up."*

***FIELD RECONCILIATION (2026-07-23) — read this before running more challenges.*** *A multi-model outbound on the established science ([`../copy research.md`](../copy%20research.md), captured in [`state-of-the-field.md`](state-of-the-field.md)) reframes the whole gate-6 effort — see [`../STATE.md`](../STATE.md) §Field reconciliation. Load-bearing: **(1)** single-site *unbounded* capacity is confirmed the **wrong target** — kinetic proofreading is temporal redundancy, gives *bounded* single-site fidelity, saturates (Toyabe cascade, asymmetric cooperativity both cap); **our folding wall is the field's known floor, not a lattice artifact.** **(2)** The real, field-corroborated open gate is **closure / self-description** (the corrector must copy itself) — TP's north star, unsettled in every field. **(3)** Corrected gate-6 invariant: entropy exported faster than errors are created, across redundant DOF, **with the corrector copied.** So: **running folding v3 now only re-confirms the floor — it is no longer the high-value move.** Higher value: metabolize the imports (**asymmetric cooperativity** = drive-scavenged proofreading, the minimal-machinery lead; **VCG** = distributed register) against the closure-corrected gate 6, and develop the **anisotropically-driven topological register** candidate top (STATE §Field reconciliation — build-to-see: topological templating + pull, drive as universal corrector). v3 stays as the record; run it only to close the impossibility formally (move A).*

*Prior state (superseded by the above but kept for the record): two folding outbounds done and graded (#6–8 v1, #9–11 v2, [`challenge-attempts.md`](challenge-attempts.md)); v2 suggested a wall but the prompt leaked (forfeits too cheap); v3 (earned-forfeit) is the fix; §After the second folding outbound has the detail.*

## What the fork session did (2026-07-22)

Turned gate 6 (the one open gate — fidelity on the tape) from a description into a **self-certifying witness challenge**, then ran it. The method: hand a solver an explicit construction task with a mechanical acceptance oracle whose conditions *are* the guards (on-tape, drive-off collapse, heritable, above Eigen). We verify by recomputing a few numbers — never by refereeing biology. This is now the project's core direction (installed in [`../CLAUDE.md`](../CLAUDE.md) and [`../STATE.md`](../STATE.md)).

**Five attempts, logged in [`challenge-attempts.md`](challenge-attempts.md):**
1. A strong solver declared C0 impossible — a **misparse** (conflated legal local-state-dependent rates with forbidden substring-addressing). Refuted; C0 sharpened.
2–3. **Two independent clamp-motif witnesses** (mine + Gemini) pass C0–C4 but hit the same **off-tape ceiling** (~`d²`). Forced condition **C5** (capacity on the tape).
4–5. **Two independent cascade attempts** to clear C5 both fail: mine needed a forbidden **sliding read-head**; Gemini's is in-bounds (emergent latching) but a **delay line** that can't stack stages (saturates, depth-independent).

**The finding these converge on:** unbounded, tape-set fidelity needs `n` *independent* proofreading stages acting on one site → `n` elements *in contact* with it → **folding**. A 1-D bounded window caps this at O(1) (~`d²`). The 1-D challenge did its job and located its own edge: it proved sequence-encoded, drive-carried fidelity is real and buildable, and proved 1-D can't reach capacity. **It is now closed** — keep [`../CHALLENGE.md`](../CHALLENGE.md) as the record; don't re-run it.

## Since the fork (2026-07-23)

- **Track A's v0 comparison is done — [`../CHALLENGE-fold.md`](../CHALLENGE-fold.md) is now v1**, the winner-merge, and the standing outbound. Base was the internal v0 (more complete oracle: a separate **G**, the known-non-qualifying list, the stage-counting procedure, C3-as-detailed-balance-identity); folded in three things from Gemini's v0 — the coordinate map promoted to a named tuple element **`(S, X, c, A, W)`** with the `‖Xᵢ−Xᵢ₋₁‖₁ = 1` / `Xᵢ ≠ Xⱼ` hand-checks, **W's blindness stated positively** (sees only spatial adjacency, never the sequence-index `i`), and the operator probe (**emit `X` before `W`**). Gemini's "C6 = self-folding" numbering was **not** adopted — that content lives in C0; TP's C6 stays reserved for von Neumann self-description.
- **STATE's gate-6 register was rebuilt import-led.** A first draft used a coined "finish trade / the weld" trade metaphor and was rejected as "too much that is ours." The kept version hands the idea to the **reliable-copying sciences** (Shannon 1948; von Neumann 1956; Hopfield 1974 / Ninio 1975; the multi-stage proofreading literature): TP imported them only as Eigen's bound, when they hold a **constructive proofreading ladder** (equilibrium floor → driven discard → independent stages → capacity-needs-folding), and gate 6 is where that ladder co-realizes with von Neumann's constructor–tape. New STATE section: *"The fidelity sciences, laid alongside the circuit."* Also added a marked basin/watershed cross-check beside the driven-multistability lead (medial axis = watershed at the massless limit only). **Register lesson: hold the [`character.md`](../../character-framework/framework/character.md) import-led standard — strip test per sentence, minimal coinage.**
- **[`../CHALLENGE inline.md`](../CHALLENGE%20inline.md) de-staled** to the folding tuple + coordinates-first. It is the framing line — **paste it as the chat message when you drag the challenge, not as a second attachment** (a dragged file with no typed prompt sends solvers meta — "there's no user prompt").

## After the first folding outbound (2026-07-23) — attempts #6–8 graded

`CHALLENGE-fold.md` v1 went to three solvers (models a/b/c, inlined in the challenge file; **identities were not recorded — record them next time**, independence from the v0 authors matters). Graded on G + C0–C5; full write-ups in [`challenge-attempts.md`](challenge-attempts.md).

**Result: no witness survives; all three fail C5 — and, notably, all three drew *valid* folding geometry (G ✓).** So the LLM 2-D-spatial-reasoning wall the fork feared **did not fire** — the failure is conceptual, not geometric.

- **#6 (model a):** honest forfeit, names C5.
- **#7 (model b):** valid 6-mer geometry, forfeits at C5 — but with **the kept contribution: the coordination bound**, now a standing gate-6 question (folded into STATE). A single lattice growth-site has ≤4 (2-D)/≤6 (3-D) neighbours, minus bonds to predecessor + template, so only ~2–4 distinct fold-contacts can act on *one* copied bit *at once* → stages are geometry-capped at a finite `d^z`, even with folding. This is a real constraint to run down: the "fold → deep pocket brings `n` contacts to one site" reading owes a mechanism for how the fold delivers `n` *independent* resets to one bit. Two live resolutions, both normal frontier now: **(i)** the resets are **sequential in time** on one bit (emergent processivity — the in-bounds kind), so `z` counts time-ordered discards, not simultaneous contacts; or **(ii)** a **finite `d^z` ceiling** is the honest answer, itself a "how little chemistry" result.
- **#8 (model c):** first folding return to *claim* a clean pass; **rejected.** Valid geometry, but fails C5 by a **new cheat — site-stacking:** a channel of single-stage sites where a longer copier just makes a longer *daughter* (more bits, each copied once), and `∏ᵢ survivalᵢ` is reported in place of `Q = minᵢ qᵢ`. Per-bit fidelity is a rule-fixed constant (~0.936); nothing scales. (Plus: C2 demo bogus — its `T_b` has agreeing walls at every site; C3 drive enters only a sequence-independent hop; C4a daughter is a 4-mer, not a self-copy.) **Now folded into CHALLENGE-fold's known-non-qualifying list.**

**What this changes:**
1. **Oracle hardened** (done): site-stacking + "drive-in-a-sequence-independent-step" added to CHALLENGE-fold's known-non-qualifying list. C5's `Q = minᵢ qᵢ` definition already catches the cheat; the list now names it.
2. **The one open judgment call (flagged, not folded — Ron/thesis-level):** does model b's coordination bound mean **C5 should be re-phrased** to demand *sequential-in-time independent driven resets on one copied bit* (emergent-processive stages), rather than the current "distinct contact in the pocket" wording that reads as *contemporaneous* contacts? If the honest answer is a finite `d^z` ceiling set by pocket coordination, that is itself a result ("how little chemistry" = "a pocket of coordination `z`, giving `d^z`"). **Don't fold speculatively** — resolve it against the next witness or by working the geometry directly (this is arguably a Track-B question now: run it in TP's substrate and *measure* the achievable `z`).

3. **CHALLENGE-fold sharpened to v2 (2026-07-23) — the decisive-outcome prompt.** Per Ron's steer ("formulate the prompt so well we either solve it or hit the conceptual wall"), v1's C5 was rewritten to close the site-stacking sidestep and force the coordination bound into the open. Three changes: **(i)** capacity is pinned to **one designated site `i*`** — lengthening the strand is explicitly disqualified (`Q = minᵢ qᵢ`); **(ii)** the evidence is a **tape family `T^{(n)}`** with per-site error falling **geometrically** `1−q*(n) ≲ d^{−n}` (the mechanical test that separates real independent stages from a saturating delay line — no biology-refereeing); **(iii)** the prompt now **states the coordination bound** (≤4/≤6 neighbours, so `n` stages can't all touch `i*` at once) and demands the solver either show the stages are time-ordered/nested *and emergent from `W`*, or **name the impossibility precisely**. C0–C4 and G unchanged. Matching edits in [`../CHALLENGE inline.md`](../CHALLENGE%20inline.md) (the framing message) and the deliverable/how-checked sections.

## After the second folding outbound (v2, 2026-07-23) — attempts #9–11 — WALL SUGGESTED, PROMPT LEAKED

v2 went to three fresh solvers (a′/b′/c′; **identities again not recorded — fix this**). Raw returns: [`external/folding-outbound-2_raw.md`](external/folding-outbound-2_raw.md); full write-ups in [`challenge-attempts.md`](challenge-attempts.md). **The take-away flipped on review (Ron's catch): two solvers took a free "it's impossible" exit that v2 itself invited, so the "clean wall" reading was over-called. The prompt leaked; v3 fixes it.**

- **#9 (a′):** honest forfeit landing precisely on the emergence requirement — knows no local `W` where the time-ordered/nested ordering *emerges* rather than from an implicit read-head.
- **#10 (b′): an armchair impossibility claim — and the tell that v2 leaks.** Spatial accounting at a copied site `X_{i*}`: 4 neighbours − 2 (chain contiguity) − 1 (daughter) = **exactly 1 free neighbour** for a fold contact (3 in 3-D). So `n` *simultaneous* contacts are impossible; sequential delivery = shifting the chain across one site = the sliding read-head C0 forbids. **But this is weak evidence, not the headline:** v2 *states* the coordination bound to the solver and *blesses* "name the impossibility" as decisive, so #10 exited for free by echoing our own prompt, **built nothing**, and **missed** the downstream-length route (#11 found it). A forfeit this cheap can't distinguish "real wall" from "didn't try."
- **#11 (c′): the only genuine attempt — claims a "backtrack ratchet" witness; rejected.** A stress flag makes a wrong-`i*` daughter depolymerize; a deeper *empty channel* gives more backtrack steps → geometric `q*(n)`. But scaling is bought by **downstream strand length** on a **homogeneous all-0 tape**: one discrimination signal amplified by a processive walk (not `n` independent resets), `n−1` discards act on *other* monomers, zero sequence info, and `Q=minᵢqᵢ` is pinned by the unprotected tail. It **evades** the coordination bound by *not* putting `n` contacts on `i*` — the disqualified length-route.

**Honest status (walked back):** the wall is **suggested, not established.** Six engagements, but only **one genuine attempt** (#11); the rest forfeited cheaply (a/b) or leaked (v1). One real failed attempt — failing by the length-evasion — is *suggestive*, not a proof. I over-called #10 as the decisive result; a wall is evidence only when hit from *inside* a real attempt.

**v3 fixes the prompt (done).** An impossibility verdict is now admissible **only** with the **earned-forfeit package**: the best explicit `(S,X,c,A,W)` you can build, its computed `q*(n)`, and the exact capping step + why it can't be escaped. The coordination bound is demoted to "suspected obstruction — resolve constructively, do not concede to it." A bare "impossible" is non-compliant (under v3, #10 is rejected). Also hardened: two new known-non-qualifying entries (**backtrack/abort ratchet**, **homogeneous-tape**) and C5/how-checked now require each stage be *delivered to `i*` by a sequence-distant monomer folded into contact*, not a longer downstream daughter.

**Next action — run v3 first; it gates everything else.**
- **Run CHALLENGE-fold v3** on a fresh solver (**record identity**), pasting [`../CHALLENGE inline.md`](../CHALLENGE%20inline.md). Log as **#12**. The question v3 answers: *does a solver forced to actually build still fail — and by the same length/slide evasion — or does genuine effort surface the emergent time-ordered `W`?* Only earned attempts are evidence. If several earned attempts converge on the same failure, **then** the wall is real and the two later moves open:
  - **(A) Formalize the impossibility** into a lattice-grammar theorem (kill each evasion: simultaneous / template-shift / downstream-length / homogeneous-tape → capacity ≤ `d^z`, `z` = free coordination). A clean negative result and itself a gate-6 answer.
  - **(B) Track B — measure `z` where it lives.** Quantitative: *is the coordination-capped `d^z` large enough to clear the Eigen bar for a real replicator, or fatal?* Measured in TP's substrate, not argued on paper. A finite ceiling that clears Eigen reframes the whole finding from "wall" to "here is exactly the chemistry price." **The higher-value move once the wall is earned.**

## The fork (this is the part to get right)

The work now **pseudo-forks** into two tracks. They share everything upstream — the gate-6 target, the guards, and the findings above — and they will reconverge (Track A's generator is the blueprint Track B runs). Same repo; distinguished by artifact.

### Track A — the folding grammar (the math proof) — LIVE, immediate
The abstract question: **does a minimal self-folding sequence-encoded catalyst exist that passes C0–C5 on a lattice?** This is the "theoretical right to exist" — the rigorous argument that a capacity-unbounded replicator can run under uniform local rules without smuggling in a machine.
- **Artifact:** [`../CHALLENGE-fold.md`](../CHALLENGE-fold.md) — **v2, the decisive-outcome prompt** (v1 was the winner-merge of two v0s; v2 sharpened C5 — see §After the first folding outbound). Gemini's parallel v0 is kept at [`external/gemini_folding-grammar-v0.md`](external/gemini_folding-grammar-v0.md) for the record.
- **First outbound done (2026-07-23) — attempts #6–8, all fail C5; see §After the first folding outbound.** Next outbound uses **v2**: drag `CHALLENGE-fold.md` to a **fresh** frontier model (not a v0 author or first-outbound solver, for independence — **record the identity**), pasting [`../CHALLENGE inline.md`](../CHALLENGE%20inline.md) **as the message**. Grade against **G + C0–C5** per §Grading a returned folding witness, and log as **#9**.
- **The live risk (Gemini flagged it):** frontier models weak on 2-D/3-D coordinates, overlaps, folding. **Update: this wall did *not* fire on the first outbound** — all three solvers drew valid self-avoiding walks with real contacts. The failure was conceptual (C5), not geometric. So the spatial-reasoning ceiling is not the current blocker; the C5 stage-stacking concept is.

### Track B — TP's geometric substrate (the evolutionary engine) — NOT STARTED, eventual
TP's native geometry — sphere caps, the medial axis, inversive geometry (the massless bit of [`../CLAUDE.md`](../CLAUDE.md)/[`../STATE.md`](../STATE.md)) — is the sandbox where you can **actually run the loop and watch lineages climb the Eigen threshold**. Track A gives a blueprint `W` on paper; only Track B lets you watch Darwin happen. Begin this when Track A either yields a clean `W` or parks at the spatial wall. This is the loop-closure: the oracle work drove the abstract challenge *back* to the geometric substrate TP started from.

## Grading a returned folding witness (attempt #6+)

When a solver returns a construction, grade in this order and log the per-condition `✓ / ✗ / — / ?` + verdict in [`challenge-attempts.md`](challenge-attempts.md). Recompute the numbers; never referee biology.

1. **G — geometry first.** From the coordinates `X`: is the walk self-avoiding (`Xᵢ ≠ Xⱼ`), are consecutive monomers adjacent (`‖Xᵢ−Xᵢ₋₁‖₁ = 1`), and is every claimed contact a real lattice-adjacency? **If coordinates overlap or contacts are invented, stop — that is the LLM spatial-reasoning wall.** Log the ceiling, park Track A, move to Track B. (Finding the wall fast is a result, not a failure.)
2. **C0.** One uniform local rate function over a bounded spatial neighbourhood; folding *emergent* (complementary latching), not imposed; **no sliding read-head / token** (attempt #4's disqualifier).
3. **C1–C2.** Read-out discrete and templated; `Q` depends on sequence content (two tapes, different `Q`).
4. **C3.** The `A = 0` detailed-balance identity — `Q` sequence-independent at zero drive. *Watch the shelved cold-baseline gap:* is `Q(A=0)` genuinely near chance, or a mostly-cold copier with a thin driven top-up (Gemini's `0.99`)? Note it; strengthen C3 only if a witness cheats through it.
5. **C4.** Heritable (copied by the same dynamics), a variant with strictly higher `Q`, and an `N` with `N·(1−Q) < 1`.
6. **C5 — the make-or-break.** Count the independent driven-discard stages: each must be a *separate, irreversible discard from a distinct contact*, and the count must scale with the folded copier **with no rule-fixed ceiling**. Reject the two known cheats explicitly: a **delay line** dressed as stacking (saturates, `n`-independent — attempt #5), and a **single-clamp** ceiling (~`d²` — attempts #2/#3).

**If it passes C0–C5 cleanly** — the first folding witness to do so — check the shelved **C6** before celebrating: does it lean on a degenerate all-one-symbol tape (self-proofreads while encoding zero information)? If so it's a von-Neumann-self-description gap, not a solution — fold C6 in. If it passes C0–C5 *and* carries real information, that is the gate-6 witness, and the move is Track B (run it).

## Shelved decisions — do not lose these

- **C6 — von Neumann self-description.** The degenerate all-`1` tape passes C0–C5 while encoding zero information. C0–C5 don't yet demand the copier be a *description that constructs the copier*. Fold C6 in when a witness actually exploits this.
- **C3 cold-baseline strengthening.** Gemini's witness passed C3's *letter* (tape-control vanishes at `A=0`) while being a mostly-cold copier (`Q(A=0)=0.99`). Strengthen C3 to demand the fidelity *itself* be drive-carried only if a witness cheats through it. (Attempt #2's `Q(A=0)=½` is the clean form.)
- **Folding-grammar open design questions.** The 2-D 4-neighbours-per-site limit may force 3-D for deep pockets; how exactly the daughter is "grown"; keeping everything hand-checkable. These are Track A's to resolve as witnesses come in.

*Discipline reminder (why this went well): each attempt passed the current oracle and thereby exposed the next gap. Harden by attacking, not by adding clauses speculatively. Convergence between independent solvers is the signal; a solver that forfeits by naming the blocking condition is a success, not a failure.*

## Where everything lives

- [`../CHALLENGE.md`](../CHALLENGE.md) — the 1-D challenge, C0–C5. **Closed** (proved its point). The record.
- [`../CHALLENGE-fold.md`](../CHALLENGE-fold.md) — Track A, the folding grammar **v3** (v2's single-site C5 + the **earned-forfeit** rule: an impossibility claim needs an exhibited failing construction). **Live — the current outbound.**
- [`../CHALLENGE inline.md`](../CHALLENGE%20inline.md) — the framing line; paste **as the message** when dragging the challenge.
- [`challenge-attempts.md`](challenge-attempts.md) — the running log (1-D #1–5 + folding v1 #6–8 + folding v2 #9–11 + evaluations + shelved gaps). **Continue at #12** only if running the move-(A) proof/refutation outbound.
- [`external/folding-outbound-1_raw.md`](external/folding-outbound-1_raw.md) / [`external/folding-outbound-2_raw.md`](external/folding-outbound-2_raw.md) — raw solver returns for the v1 / v2 outbounds (extracted so the challenge file stays clean).
- [`../STATE.md`](../STATE.md) — the thinking; the fidelity-sciences reframe of gate 6, the basin/watershed cross-check, frontier, the oracle's edges, the folding finding, the fork.
- [`../CLAUDE.md`](../CLAUDE.md) — the guards + the gate-6 method.
- [`../where-we-are.md`](../where-we-are.md) — the visualization brief (still 1-D-framed; would want a folding panel + the C5/capacity story added before the next hand-off to a video/infographic model).
